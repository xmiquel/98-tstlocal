# 98-tstlocal

FastAPI web app for trading strategy creation, backtesting, and tracking.

## Status

Greenfield. Python 3.12 + uv + pytest + ruff + mypy strict. See `openspec/specs/python-toolchain/spec.md` for the toolchain contract.
