# Tasks: chart-visualization

## Phase 1: CSS Tooltip Implementation

- [x] 1.1 Add tooltip CSS to `static/css/app.css` (dark theme, match #131722 bg, #d1d4dc text)
- [x] 1.2 Run `uv run pytest` — ensure 64 tests still pass
- [x] 1.3 Run `ruff check . && ruff format --check . && mypy .` — all clean
