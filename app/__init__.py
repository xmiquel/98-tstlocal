# Package marker for the FastAPI trading application.
# The application code lands in subsequent changes; the marker exists today
# so `pytest --cov=app` has an importable module to measure and the 80%
# coverage gate is satisfiable from the very first commit.
