# Python — virtualenv, caches, bytecode, build, coverage, packaging
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
*.cover
.uv-cache/

# User-level IDE / agent workflow artifacts (not part of the project source)
.atl/
.windsurf/

# SQLite database files
*.db
data/

# Playwright browser cache — installed by `uv run playwright install chromium`
ms-playwright/
playwright/

# Environment — local overrides (never committed)
.env
